<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Danh sách sản phẩm</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
            background: #f3f4f6;
            margin: 0;
        }

        /* ===== NAVBAR ===== */
        .navbar {
            background: #111827;
            padding: 14px 40px;
            display: flex;
            gap: 24px;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0 2px 10px rgba(0, 0, 0, .15);
        }

        .navbar a {
            color: #e5e7eb;
            text-decoration: none;
            font-weight: 600;
            padding: 6px 12px;
            border-radius: 6px;
            transition: .2s;
        }

        .navbar a:hover {
            background: #2563eb;
            color: #fff;
        }

        /* ===== SEARCH ===== */
        #searchInput {
            margin: 25px 40px 15px;
            padding: 12px 14px;
            width: 320px;
            border-radius: 8px;
            border: 1px solid #d1d5db;
            outline: none;
        }

        #searchInput:focus {
            border-color: #2563eb;
            box-shadow: 0 0 0 2px rgba(37, 99, 235, .2);
        }

        /* ===== PRODUCT GRID ===== */
        .product-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(230px, 1fr));
            gap: 22px;
            padding: 0 40px 40px;
        }

        /* ===== PRODUCT CARD ===== */
        .product {
            background: #fff;
            border-radius: 14px;
            box-shadow: 0 4px 14px rgba(0, 0, 0, .08);
            padding: 14px;
            transition: .2s;
        }

        .product:hover {
            transform: translateY(-4px);
            box-shadow: 0 10px 22px rgba(0, 0, 0, .15);
        }

        .product img {
            width: 100%;
            height: 170px;
            object-fit: cover;
            border-radius: 10px;
            margin-bottom: 8px;
        }

        .product h3 {
            font-size: 16px;
            margin: 6px 0;
        }

        /* ===== PRICE ===== */
        .price {
            color: #dc2626;
            font-size: 18px;
            font-weight: 700;
        }

        /* ===== UNIT SELECT ===== */
        .product select {
            width: 100%;
            padding: 6px 8px;
            border-radius: 6px;
            border: 1px solid #d1d5db;
            margin: 6px 0;
        }

        /* ===== ACTION ===== */
        .action-buttons {
            display: flex;
            gap: 8px;
            margin: 8px 0;
        }

        .btn-edit {
            background: #2563eb;
            color: #fff;
            padding: 6px 10px;
            border-radius: 6px;
            font-size: 13px;
            text-decoration: none;
        }

        .btn-edit:hover {
            background: #1d4ed8;
        }

        .btn-delete {
            background: #dc2626;
            color: #fff;
            padding: 6px 10px;
            border-radius: 6px;
            border: none;
            cursor: pointer;
        }

        .btn-delete:hover {
            background: #b91c1c;
        }

        /* ===== CART BOX ===== */
        .cart-box {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .qty-btn {
            width: 30px;
            height: 30px;
            border: none;
            background: #e5e7eb;
            border-radius: 6px;
            font-size: 18px;
            cursor: pointer;
        }

        .qty-btn:hover {
            background: #d1d5db;
        }

        .qty-input {
            width: 40px;
            text-align: center;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            height: 30px;
        }

        .btn-cart {
            margin-left: auto;
            background: #22c55e;
            color: #fff;
            border: none;
            padding: 6px 12px;
            border-radius: 8px;
            font-size: 16px;
            cursor: pointer;
        }

        .btn-cart:hover {
            background: #16a34a;
        }

        .nav-left {
            display: flex;
            align-items: center;
            gap: 10px;
        }
.menu-btn {
            font-size: 22px;
            color: #fff;
            cursor: pointer;
            display: none;
        }   
        /* .logo {
            color: #fff;
            font-weight: bold;
        } */

        

        @media (max-width: 768px) {

            .navbar {
                flex-wrap: wrap;
                gap: 10px;
                padding: 10px 14px;
            }

            .navbar a {
                flex: 1 1 45%;
                text-align: center;
                font-size: 14px;
                padding: 8px 0;
            }

            #searchInput {
                width: calc(100% - 30px);
                margin: 16px 15px;
            }

            h1 {
                font-size: 20px;
                margin-left: 15px;
            }

            .product-list {
                padding: 0 12px 20px;
                grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
                gap: 12px;
            }

            .product {
                padding: 10px;
                border-radius: 10px;
            }

            .product img {
                height: 120px;
            }

            .product h3 {
                font-size: 14px;
            }

            .price {
                font-size: 16px;
            }

            .cart-box {
                flex-wrap: wrap;
                gap: 6px;
            }

            .qty-input {
                /* width: 100%; */
            }

            .btn-cart {
                width: 100%;
                margin-left: 0;
                padding: 10px 0;
            }

            .menu-btn {
                display: block;
                color:white

            }

            .navbar {
                padding: 12px 14px;
                justify-content: space-between;
            }

            .nav-links {
                position: absolute;
                top: 55px;
                /* left: 0; */
                width: 100%;
                background: #111827;
                flex-direction: column;
                display: none;
            }

            .nav-links a {
                padding: 12px;
                border-top: 1px solid #1f2937;
            }

            .nav-links.show {
                display: flex;
            }
        }
    </style>

</head>

<body>
    <%- include('../partials/navbar') %>


        <input type="text" id="searchInput" placeholder="🔍 Nhấn / để tìm nhanh..."
            style="padding:10px; width:300px; margin-bottom:20px; margin-left:40px;" onkeyup="searchProduct()">

        <hr>

        <h1>📦 Danh sách sản phẩm</h1>

        <div class="product-list">

            <% products.forEach(product=> { %>
                <div class="product" data-name="<%= product.name %>" data-barcode="<%= product.barcode || '' %>">

                    <!-- Hình ảnh -->
                    <img src="<%= product.image_url %>" alt="<%= product.name %>">
                    <!-- <img src="/uploads/products/cocacola.jpg" alt="cocacola"> -->


                    <!-- Tên -->
                    <h3>
                        <%= product.name %>
                    </h3>

                    <!-- Chọn đơn vị -->
                    <label>Chọn đơn vị bán:</label>
                    <select onchange="changePrice(this)">
                        <% product.units.forEach(unit=> { %>
                            <option value="<%= unit.price_sell %>">
                                <%= unit.unit_name %> - <%= unit.price_sell.toLocaleString() %> đ
                            </option>
                            <% }) %>
                    </select>

                    <!-- Giá -->
                    <p>
                        Thành tiền:
                        <span class="price">0</span> đ
                    </p>
                    <div class="action-buttons">
                        <a class="btn-edit" href="/products/edit/<%= product.id %>">✏️ Sửa</a>

                        <form method="POST" action="/products/delete/<%= product.id %>"
                            onsubmit="return confirm('Bạn có chắc muốn xoá sản phẩm này không?');">
                            <!-- <button type="submit" class="btn-delete">🗑 Xoá</button> -->
                        </form>
                    </div>
                    <div class="cart-box">
                        <button class="qty-btn" onclick="changeQty(this, -1)">-</button>
                        <input class="qty-input" type="text" value="1">
                        <button class="qty-btn" onclick="changeQty(this, 1)">+</button>

                        <button class="btn-cart" onclick="addToCart(this)"><ion-icon
                                name="add-outline"></ion-icon></button>
                    </div>


                </div>
                <% }) %>

        </div>




        <script>
            function changePrice(select) {
                const product = select.closest(".product");
                const unitPrice = Number(select.value);
                const qty = Number(product.querySelector(".qty-input").value || 1);
                const priceSpan = product.querySelector(".price");

                const total = unitPrice * qty;
                priceSpan.innerText = total.toLocaleString();
            }
        </script>

        <script>
            // Hàm bỏ dấu tiếng Việt
            function removeVietnameseTones(str) {
                return str
                    .toLowerCase()
                    .normalize("NFD")
                    .replace(/[\u0300-\u036f]/g, "")
                    .replace(/đ/g, "d");
            }

            function searchProduct() {
                const keyword = removeVietnameseTones(
                    document.getElementById("searchInput").value
                );

                const products = document.querySelectorAll(".product");

                products.forEach(product => {
                    const name = removeVietnameseTones(product.dataset.name);
                    const barcode = removeVietnameseTones(product.dataset.barcode);

                    if (name.includes(keyword) || barcode.includes(keyword)) {
                        product.style.display = "block";
                    } else {
                        product.style.display = "none";
                    }
                });
            }

            // function changePrice(select) {
            //     const price = select.value;
            //     const priceSpan = select.parentElement.querySelector(".price");
            //     priceSpan.innerText = Number(price).toLocaleString();
            // }
        </script>
        <script>
            function changeQty(btn, value) {
                const product = btn.closest(".product");
                const input = product.querySelector(".qty-input");

                let qty = parseInt(input.value) || 1;
                qty += value;
                if (qty < 1) qty = 1;
                input.value = qty;

                // cập nhật lại thành tiền
                const select = product.querySelector("select");
                changePrice(select);
            }
        </script>
        <script>
            window.onload = function () {
                document.querySelectorAll(".product select").forEach(select => {
                    changePrice(select);
                });
            };
        </script>
        <script>
            document.addEventListener("keydown", function (e) {

                // Nếu đang gõ trong input khác thì bỏ qua
                if (document.activeElement.tagName === "INPUT" ||
                    document.activeElement.tagName === "TEXTAREA") return;

                if (e.key === "`" || e.key === "/") {
                    e.preventDefault(); // không cho gõ dấu /
                    const input = document.getElementById("searchInput");
                    input.focus();
                    input.select();
                }
            });
        </script>

        <script>
            function addToCart(btn) {
                const product = btn.closest(".product");
                const name = product.dataset.name;
                const select = product.querySelector("select");
                const unitName = select.options[select.selectedIndex].text;
                const qty = product.querySelector(".qty-input").value;
                const total = product.querySelector(".price").innerText;

                alert(`🛒 Đã thêm:
${name}
${unitName}
Số lượng: ${qty}
Thành tiền: ${total} đ`);
            }
        </script>

        <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
        <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>


        <script>
            function toggleMenu() {
                document.getElementById("navLinks").classList.toggle("show");
            }
        </script>

</body>

</html>