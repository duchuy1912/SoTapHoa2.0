<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Sửa sản phẩm</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f3f4f6;
      padding: 30px;
    }

    .container {
      background: #fff;
      max-width: 520px;
      margin: auto;
      padding: 25px 30px;
      border-radius: 10px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, .1);
    }

    h2 {
      margin-bottom: 20px;
      color: #111827;
    }

    label {
      font-weight: 600;
      display: block;
      margin-top: 12px;
    }

    input {
      width: 100%;
      padding: 8px 10px;
      margin-top: 6px;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 14px;
    }

    .unit-row {
      display: flex;
      gap: 8px;
      margin-bottom: 8px;
    }

    .unit-row input {
      flex: 1;
    }

    .btn {
      margin-top: 18px;
      padding: 10px 15px;
      border: none;
      border-radius: 6px;
      font-weight: bold;
      cursor: pointer;
    }

    .btn-save {
      background: #22c55e;
      color: #fff;
    }

    .btn-save:hover {
      background: #16a34a;
    }

    .btn-back {
      background: #6b7280;
      color: #fff;
      margin-left: 10px;
      text-decoration: none;
    }

    .btn-back:hover {
      background: #4b5563;
    }

    .btn-add {
      background: #3b82f6;
      color: #fff;
      margin-bottom: 12px;
    }

    .btn-add:hover {
      background: #2563eb;
    }

    .btn-remove {
      background: #ef4444;
      color: #fff;
      border: none;
      border-radius: 6px;
      padding: 0 10px;
      cursor: pointer;
    }

    .btn-remove:hover {
      background: #dc2626;
    }
  </style>
</head>

<body>

  <div class="container">
    <h2>✏️ Sửa sản phẩm</h2>

    <form action="/products/edit/<%= product.id %>" method="POST" enctype="multipart/form-data">

      <label>Tên sản phẩm</label>
      <input name="name" value="<%= product.name %>" required>

      <label>Barcode</label>
      <input name="barcode" value="<%= product.barcode %>">

      <label>Đổi hình ảnh (nếu cần)</label>
      <input type="file" name="image">
      <input type="hidden" name="old_image" value="<%= product.image_url %>">

      <hr style="margin:18px 0">

      <h3>Đơn vị bán</h3>


      <% product.units.forEach(unit=> { %>
        <div class="unit-row">
          <input type="hidden" name="unit_id[]" value="<%= unit.id %>">
          <input name="unit_name[]" value="<%= unit.unit_name %>" placeholder="Hộp / Lốc / Thùng">
          <input name="price_sell[]" value="<%= unit.price_sell %>" placeholder="Giá">
          <button type="button" class="btn-remove" onclick="removeUnit(this)">✖</button>
        </div>
        <% }) %>
          <button type="button" class="btn btn-add" onclick="addUnit()">➕ Thêm đơn vị</button> <br>
          <button class="btn btn-save">💾 Cập nhật</button>
          <a href="/" class="btn btn-back">↩ Quay lại</a>

    </form>
  </div>
  <script>
    function addUnit() {
      const div = document.createElement("div");
      div.className = "unit-row";
      div.innerHTML = `
    <input type="hidden" name="unit_id[]" value="">
    <input name="unit_name[]" placeholder="Hộp / Lốc / Thùng">
    <input name="price_sell[]" placeholder="Giá">
    <button type="button" class="btn-remove" onclick="removeUnit(this)">✖</button>
  `;
      document.querySelector(".container").querySelector("h3").after(div);
    }

    function removeUnit(btn) {
      btn.parentElement.remove();
    }
  </script>

</body>

</html>